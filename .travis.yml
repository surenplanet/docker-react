language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t suren2787/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true suren2787/docker-react npm run test
